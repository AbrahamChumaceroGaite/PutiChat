version: "3.9"
services:
  putichat:
    build: 
      context: .
      dockerfile: Dockerfile  
    ports:
      - "8888:7777"
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
